# nbycomp.ops.rocky_base_rke tasks

This directory have all tasks that are executed during role application. Packages should go in `packages.yml` file. Also the package list is alphabetically ordered.
